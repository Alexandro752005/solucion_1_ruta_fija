package pe.rutafija.operation.domain;

/** Defines whether the CRM schedules directly or awaits a real driver response. */
public enum AssignmentResponseMode {
    ADMIN_DIRECT,
    MOBILE_CONFIRMATION
}
