# Plantilla privada para la cuenta administrativa de bootstrap PostgreSQL.
# Solo la usan scripts de administracion local; nunca Spring Boot ni el CRM.
# No guarde secretos en este archivo versionado.

RUTA_FIJA_BOOTSTRAP_HOST=127.0.0.1
RUTA_FIJA_BOOTSTRAP_PORT=5432
RUTA_FIJA_BOOTSTRAP_USERNAME=REEMPLAZAR_SOLO_EN_ARCHIVO_LOCAL
RUTA_FIJA_BOOTSTRAP_PASSWORD=REEMPLAZAR_SOLO_EN_ARCHIVO_LOCAL
